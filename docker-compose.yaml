services:
  postgres:
    image: postgres:17
    shm_size: "4gb"
    container_name: Streakly_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres:
    driver: local
